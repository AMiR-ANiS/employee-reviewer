<tr>
  <td style="text-align: center"><%= n %></td>
  <td style="text-align: center">
    <% for(let j = 0; j < review.stars; j++){ %> ★ <% } %>
  </td>
  <td><%= review.text %></td>
  <td><%= employee.name %></td>
  <td><%= employee.email %></td>
  <td style="text-align: center">
    <a href="/admin/assign-employee-page?id=<%= review._id %>&sort=name">
      <i class="fa-solid fa-users"></i>
    </a>
  </td>
</tr>
